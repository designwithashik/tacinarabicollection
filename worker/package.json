{
  "name": "tacin-api-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit",
    "migrate": "wrangler d1 execute tacin_db --file=./schema.sql",
    "assert:cors-config": "node ./scripts/assert-cors-config.mjs"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.9.2",
    "wrangler": "^4.42.0"
  }
}
